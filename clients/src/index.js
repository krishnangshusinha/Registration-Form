import React from 'react';
import ReactDOM from 'react-dom';
import App from './App.jsx';
/*
    npm i bootstrap
    npm i axios               --> This popular library is used to communicate with the backend. 
    npm i react-router-dom
*/
ReactDOM.render(
  <App/>,
  document.getElementById('root')
);